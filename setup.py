from distutils.core import setup

setup(
    name='Menu cycle checker',
    version='0.1dev',
    packages=['menu_cycle_checker',],
    license='The Unlicense',
    long_description='Check a menu for cycles'

)
